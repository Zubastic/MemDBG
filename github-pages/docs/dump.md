After selecting a process and loading its maps, choose a readable map and use **Dump selected map**. Pick an explicit path. Dumps are useful for offline analysis, version comparison, and AOB signature creation.

> **Watch the size:** Some maps can be very large. If the dump fails, try a smaller region or verify permissions on the selected map.
